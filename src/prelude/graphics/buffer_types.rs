pub enum BufferType {
    Vertex,
    Index,
}

pub enum BufferDataType {
    UByte,
    Uint16,
    Uint32,
    Byte,
    Int16,
    Int32,
    Float32,
}

pub enum FillType {
    Single,
    Multi,
}
